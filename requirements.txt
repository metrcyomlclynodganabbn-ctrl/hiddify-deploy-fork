# Hiddify Bot v5.0.0 - Python Dependencies (Aiogram 3)
# Updated for async architecture with PostgreSQL, Payments, Monitoring

# === Telegram ===
aiogram>=3.5.0,<4.0.0
pyTelegramBotAPI>=4.14.0  # Legacy telebot (for backward compatibility)

# === Database ===
asyncpg>=0.29.0                # PostgreSQL async driver
sqlalchemy>=2.0.0,<3.0.0     # SQLAlchemy 2.0 async ORM
alembic>=1.12.0                 # Database migrations

# === Data Validation ===
pydantic>=2.0.0
pydantic-settings>=2.0.0        # Settings management

# === HTTP Client ===
httpx>=0.25.0                   # Async HTTP client (for Hiddify API, CryptoBot)

# === Redis ===
redis>=5.0.0                    # Redis client

# === Payments ===
# CryptoBot (uses httpx)
# Telegram Stars (built into aiogram)

# === Monitoring ===
# Prometheus metrics (optional)
# prometheus-client>=0.19.0

# === Webhook Server ===
aiohttp>=3.9.0                  # Async web server for webhooks

# === Testing ===
pytest>=7.0.0
pytest-asyncio>=0.21.0          # Async test support
pytest-cov>=4.0.0               # Code coverage

# === Logging ===
loguru>=0.7.0                   # Advanced logging (optional)

# === Utilities ===
python-dotenv>=1.0.0             # Environment variables from .env

# Сессия проекта: hiddify-deploy-fork

**Последнее обновление**: 2026-02-26 17:05
**Статус**: 🟢 v3.1 в продакшене (с hotfix)
**Версия**: v2.2.0 → v3.0.0 → v3.1.0

---

## 🔑 Быстрые триггеры

Используй любую из этих фраз для быстрого восстановления контекста:

```
"продолжим работу над vpn"
"продолжаем разработку ВПН сервиса"
"давай продолжим работу с vpn ботом"
"вернёмся к vpn боту"
"продолжай hiddify"
"работа над vpn"
```

---

## Текущая задача

Рефакторинг Hiddify VPN Bot v3.0 - полная переработка админки и интеграция с Hiddify Manager API.

**Бот**: @SKRTvpnbot
**Сервер**: kodu-3xui (5.45.114.73)

---

## Что сделано (v3.0)

### ✅ Phase 1: Hiddify API Client
- **Файл**: `scripts/hiddify_api.py` (388 строк, создан)
- Класс `HiddifyAPI` для работы с Hiddify Manager API
- Методы: create_user(), get_users(), get_stats(), get_user_connections()
- Функции для инвайтов: validate_invite_code(), use_invite_code(), create_invite_code()

### ✅ Phase 2: Админка - работает
- **👥 Пользователи**: Показывает до 50 пользователей с деталями
- **➕ Создать юзера**: РЕАЛЬНОЕ создание через админку (SQLite + API)
  - Используется `telegram_id = 0` для созданных админом пользователей
  - Активация при первом `/start` от пользователя
- **📈 Статистика**: Показывает реальную статистику из SQLite + Hiddify API
  - Всего пользователей, активных, trial, заблокированных
  - Трафик за сегодня/месяц (если API доступен)

### ✅ Phase 3: Кнопки управления пользователями
Все 5 кнопок под каждым пользователем в админке:
- **📋 Инфо** - полная информация о пользователе
- **🔑 Продлить** - +30 дней к подписке
- **📦 Лимит** - +50 GB к трафику
- **🔒 Блок** - заблокировать/разблокировать
- **🗑️ Удалить** - удалить пользователя

### ✅ Phase 4: Инвайты - работает
- Валидация инвайт-кодов при `/start`
- Увеличение счётчика использований
- Показ количества приглашённых в "Пригласить друга"

### ✅ Bug fixes
- Исправлен `NOT NULL constraint failed` при создании юзера
- Исправлен `can't parse entities: Character '!' is reserved`
- Перешёл с MarkdownV2 на обычный Markdown
- UUID сокращён для отображения: `12345678...abcd`

---

### ✅ Рефакторинг v3.1: Система ролей (В ПРОДАКШЕНЕ)
- **Миграция БД**: `migrations/v3.1_add_roles.sql` - колонка role
- **Модуль ролей**: `scripts/roles.py` (270 строк)
  - Role enum (USER, MANAGER, ADMIN)
  - Функции: get_user_role(), is_admin(), is_manager(), can_invite_users()
  - set_user_role(), get_users_by_role(), get_role_display_name()
- **Обновлен бот**: `scripts/monitor_bot.py` - интеграция с ролями
- **Скрипт миграции**: `scripts/migrate_to_v31.py` - автоматическое применение
- **Тесты**: `tests/test_roles.py` (315 строк, 20+ тестов)
- **Документация**: `docs/REFACTORING_V3.1_ROLES.md`

**Hotfix (2026-02-26)**:
- Исправлен путь к БД: `users.db` → `data/bot.db`
- Исправлена типизация в `get_users_by_role` (поддержка Role и str)
- Обнаружено при деплое на kodu-3xui

**Используемые материалы из ai-knowledge-library:**
- 📘 WORKFLOW-GUIDE template для docs/fix-invited-stats.md
- 📘 coding.md PCTF framework для разработки
- 📘 AGENT-PROMPT template для системного промпта

---

## Где остановились

**Деплой**: ✅ v3.1 в продакшене на сервере kodu-3xui
**Статус**: 🟢 Production ready

**Рефакторинг v3.1 завершён**:
```
✅ Система ролей (role column)
✅ Модуль scripts/roles.py (с hotfix)
✅ Тесты tests/test_roles.py
✅ Миграция применена (2 пользователя)
✅ Админ @kapyshonchik имеет роль admin
✅ Сервис активен (systemd)
```

**Последний деплой (2026-02-26 17:00)**:
- Бэкап БД: `bot.db.backup-20260226-165716`
- Миграция применена вручную (SQL issue)
- Hotfix для roles.py (путь к БД + типизация)
- Git tag: v3.1.0

**Следующие шаги:**
1. Создать UI для управления ролями в админ-панели (v3.1.1)
2. Добавить кнопки управления ролями под пользователями
3. Ограничить функционал "Пригласить друга" для обычных пользователей
4. Исправить баг со статистикой у приглашённых пользователей

---

## Планируется (v3.1)

### Баг fixes:
- [ ] У приглашенных пользователей не отображается статистика подписок (сообщено пользователем)

### Новые функции:
1. **Отключить инвайты для обычных пользователей**
   - Только admin/manager могут приглашать
   - Убрать или скрыть "Пригласить друга" для invited users

2. **Система менеджеров**
   - Новая роль: manager (отличается от user и admin)
   - Manager может приглашать только users (не managers)
   - Admin может приглашать и managers, и users
   - При создании пользователя админом - спрашивать тип: manager или user
   - Отправка уведомления приглашённому (или возможность для админа отправить личное сообщение)

3. **Запланировать на будущее** (функционал в разработке):
   - 🔧 Управление сервером
   - 📢 Рассылка уведомлений
   - ⚙️ Настройки сервера

---

## Важные файлы

### Основной код:
- `scripts/monitor_bot.py` (1848 строк) - главный бот
  - Обработчики: admin, user, callbacks
  - FSM система для состояний
  - Валидация username, IP/доменов

- `scripts/hiddify_api.py` (388 строк) - API клиент
  - HiddifyAPI класс
  - Обработка ошибок (ConnectionError, AuthError)
  - Graceful degradation при недоступности API

- `scripts/roles.py` (264 строк) - v3.1: Система ролей
  - Role enum (USER, MANAGER, ADMIN)
  - Функции проверки прав: get_user_role(), is_admin(), is_manager(), can_invite_users()
  - set_user_role(), get_users_by_role(), get_role_display_name()
  - migrate_admin_to_role() - миграция админа

### Конфигурация:
- `.env` - переменные окружения (НЕ КОММИТИТЬ)
- `.env.example` - шаблон конфигурации

### Деплой:
- `scripts/deploy.sh` - скрипт деплоя на сервер
- `systemd/hiddify-bot.service` - systemd unit файл
- SSH: `ssh kodu-3xui` (ключ: ~/.ssh/id_ed25519_fastpanel)

---

## Ключевые решения (Architecture)

### 1. Создание пользователей админом
**Проблема**: Админ создаёт пользователя, но того ещё нет в Telegram.
**Решение**: Использовать `telegram_id = 0` как временное значение.
**Активация**: При первом `/start` пользователя обновить `telegram_id` на реальный.

### 2. Markdown vs MarkdownV2
**Проблема**: MarkdownV2 требует экранирования многих символов (`!`, `_`, `*`, etc.)
**Решение**: Использовать обычный Markdown везде.

### 3. Graceful degradation
**Принцип**: Бот должен работать даже если Hiddify API недоступен.
**Реализация**: Все API вызовы в try/except, fallback на локальную БД.

### 4. UUID в сообщениях
**Проблема**: Полный UUID ломает Markdown (символы `_`).
**Решение**: Показывать сокращённый UUID: `12345678...abcd`

### 5. Роли пользователей
**Текущее состояние**:
- Admin: `telegram_id == TELEGRAM_ADMIN_ID` (из .env)
- User: все остальные
- `user_type` column есть в БД но не используется

**Планы v3.1**: Добавить роль manager с ограниченными правами.

---

## Следующие шаги

1. **Дождаться фидбека от пользователя** по v3.0
2. **Исправить баг** со статистикой у приглашенных пользователей
3. **Реализовать систему менеджеров** v3.1:
   - Миграция БД: добавить колонку `role` ('user', 'manager', 'admin')
   - Обновить create_user_flow для выбора типа
   - Добавить проверки прав для инвайтов
4. **Запланировать** будущие функции (server management, broadcast, settings)

---

## Полезные команды

### Деплой:
```bash
# На сервер
ssh kodu-3xui
systemctl restart hiddify-bot
systemctl status hiddify-bot
journalctl -u hiddify-bot -f

# Логи
tail -f /opt/hiddify-manager/logs/bot.log
```

### Разработка:
```bash
# Тесты
make test

# Линтинг
make lint

# Деплой
make deploy
```

---

## Git workflow

### Основная ветка: `main`
### Теги: `v*` (v3.0.0, v3.1.0, etc.)

### Для feature работы:
```bash
git checkout -b feature/manager-system
# ... работа ...
git push origin feature/manager-system
# создать PR
```

---

## Серверная информация

**kodu-3xui** (5.45.114.73)
- Провайдер: Kodu.host
- RAM: 3.9GB total, 423MB used
- Disk: 40GB total, 2.6GB used
- Путь: `/opt/hiddify-manager/`
- venv: `/opt/hiddify-manager/venv/`

**SSH доступ**:
- Alias: `kodu-3xui` или `3xui`
- Ключ: `~/.ssh/id_ed25519_fastpanel`
- Пароль: в `~/.mcp-env` (KODU_3XUI_PASSWORD)

---

## История версий

### v3.0.0 (2026-02-26) - Полный рефакторинг
- ✅ Hiddify API Client
- ✅ Рабочая админка
- ✅ Кнопки управления пользователями
- ✅ Валидация инвайтов
- ✅ Мои устройства через API

### v2.2.0 - CI/CD pipeline
- GitHub Actions
- Интеграционные тесты
- Quality checks (flake8, mypy, bandit)

### v2.1.0 - QR коды, инструкции, trial
- QR код генерация
- Инструкции для платформ
- Пробный период (7 дней, 10 GB)

### v2.0.0 - Telegram бот v2
- Приватные пользователи
- Админ-панель
- SQLite БД

---

## Заметки

- Hiddify Manager API токен не настроен в .env (если есть - добавить)
- Для продакшена: настроить SSL для Hiddify Panel
- В будущем: мониторинг (Prometheus/Grafana), автоматические бэкапы

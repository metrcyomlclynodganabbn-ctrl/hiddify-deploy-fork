# 🤖 ИНСТРУКЦИЯ ДЛЯ AI-АГЕНТА: Hiddify Manager Auto-Deploy (RU-Optimized)

> **Цель**: Полностью автономное развёртывание Hiddify Manager v8 с поддержкой VLESS-Reality, Hysteria2, SS-2022, Telegram-бот админки. Оптимизация для работы в РФ, 200+ пользователей.

---

## ⚠️ ПРАВИЛА ВЫПОЛНЕНИЯ

1. **НЕ ЗАДАВАЙ ВОПРОСОВ**. Все данные — в `.env`. Если параметр отсутствует — используй безопасные дефолты.
2. **ПОРЯДОК ВЫПОЛНЕНИЯ**: Строго следуй нумерации задач в `tasks/00_*.md` → `tasks/05_*.md`.
3. **ПРОВЕРКА ПЕРЕД ДЕЙСТВИЕМ**: Перед каждой командой на VPS — проверяй доступ, версию ОС, свободное место.
4. **ОШИБКИ**: При ошибке — логируй в `logs/deploy.log`, пробуй альтернативный метод (max 2 попытки), продолжай.
5. **ОРКЕСТРАЦИЯ**: Если задача параллелизуется (напр., создание 200 пользователей) — используй subprocess/pool.

---

## 🚀 ПОШАГОВЫЙ ПАЙПЛАЙН

### ▶️ ЭТАП 0: Инициализация (`tasks/00_init.md`)
- [ ] Прочитать `.env`, валидировать обязательные поля
- [ ] Проверить SSH-доступ к VPS: `ssh -i $KEY -p $PORT $USER@$IP "uname -a"`
- [ ] Убедиться: Ubuntu 22.04, минимум 4GB RAM, 2 vCPU (для 200+ юзеров)
- [ ] Установить локальные утилиты: `jq`, `yq`, `openssh-client`

### ▶️ ЭТАП 1: Установка Hiddify Manager v8 (`tasks/01_install.md`)
```bash
# На VPS:
apt update && apt upgrade -y
pip install python-pip
pip install pyTelegramBotAPI==4.22.0
bash <(curl -s https://i.hiddify.com/v8.99.0)
```
- [ ] Дождаться завершения установки (~15 мин)
- [ ] Сохранить временный пароль панели из вывода скрипта

### ▶️ ЭТАП 2: Настройка протоколов (`tasks/02_protocols.md`)
#### VLESS-Reality (приоритет для РФ)
```json
{
  "dest": "www.apple.com:443",
  "serverNames": ["apple.com", "www.apple.com"],
  "privateKey": "<генерировать через xray x25519>",
  "shortIds": [""],
  "flow": "xtls-rprx-vision"
}
```
- Использовать `dest` с TLS 1.3 + H2, географически близкий к VPS
- `serverNames` — только high-reputation домены (apple.com, microsoft.com)

#### Hysteria2 (для мобильных/потерянных каналов)
```yaml
# configs/hysteria2_presets.yaml
up_mbps: 100
down_mbps: 100
obfs: salamander
obfs_password: auto_generate
quic:
  initStreamReceiveWindow: 8388608
  maxStreamReceiveWindow: 8388608
  initConnReceiveWindow: 20971520
  maxConnReceiveWindow: 20971520
  maxIdleTimeout: 30s
  keepAlivePeriod: 10s
```

#### Shadowsocks-2022 (резерв)
- Использовать AEAD-2022 с BLAKE3-KDF
- Включить `session-UDP` и `EIH` для мультиклиентности

#### WireGuard с обфускацией
- Включить только если `ENABLE_WIREGUARD_OBFUSCATED=1`
- Использовать режим маскировки под QUIC/DNS

### ▶️ ЭТАП 3: Telegram-бот (`tasks/03_telegram.md`)
1. Бот уже создан через @BotFather, токен в `.env`
2. В панели Hiddify:
   - Settings → Advanced → Telegram Bot Token: вставить `TELEGRAM_BOT_TOKEN`
   - Admin Telegram ID: `TELEGRAM_ADMIN_ID`
   - Включить "Telegram Proxy" если `TELEGRAM_PROXY_ENABLED=true`
3. Протестировать команды: `/start`, `/users`, `/stats`

### ▶️ ЭТАП 4: Массовое создание пользователей (`tasks/04_users.md`)
```python
# Псевдокод для генерации 200+ пользователей
for i in range(1, MAX_USERS+1):
    api.create_user(
        username=f"user_{i:03d}",
        protocols=["vless_reality", "hysteria2"],
        data_limit=BANDWIDTH_LIMIT_PER_USER_GB * 1024**3,
        expire_days=30
    )
```
- Использовать API Hiddify (не веб-интерфейс)
- Добавить rate-limit: 5 запросов/секунду
- Сохранить список ссылок в `output/subscription_links.txt`

### ▶️ ЭТАП 5: Валидация (`tasks/05_validate.md`)
- [ ] Запустить `scripts/validate_connection.sh` из РФ (через прокси-чекер)
- [ ] Проверить:
  - VLESS-Reality: TLS-fingerprint совпадает с apple.com
  - Hysteria2: работает на UDP, нет блокировок по QUIC
  - Telegram-бот: отвечает на `/ping`, показывает статистику
- [ ] Логировать результаты в `logs/deploy.log`

---

## 🛡️ RU-СПЕЦИФИЧНЫЕ ОПТИМИЗАЦИИ (2026)

1. **REALITY**: Использовать `uTLS` с fingerprint `chrome` или `ios` для минимизации детекта
2. **Hysteria2**: Включить `Brutal` congestion control для стабильности на мобильных сетях РФ
3. **Авто-ротация доменов**: Если `PANEL_DOMAIN` блокируется — агент должен уметь переключать на зеркало (добавить в `.env` `BACKUP_DOMAINS=alt1.yourdomain.ru,alt2.yourdomain.ru`)
4. **Анти-зондирование**: Включить в Xray `sniffing.destOverride: ["http", "tls", "quic"]` для обхода активного сканирования

---

## 🧰 ПОЛЕЗНЫЕ КОМАНДЫ ДЛЯ АГЕНТА

```bash
# Генерация ключей для REALITY
ssh $VPS "xray x25519" | tee reality_keys.txt

# Проверка статуса Hiddify
ssh $VPS "systemctl status hiddify"

# Экспорт конфигурации для клиентов
ssh $VPS "hiddify-cli export --format=clash --output=/tmp/config.yaml"

# Мониторинг нагрузки
ssh $VPS "htop -b -n 1 | grep -E 'Mem|CPU'"
```

---

## 🚨 ОБРАБОТКА ОШИБОК

| Ошибка | Действие |
|--------|----------|
| SSH timeout | Проверить IP/port/key, попробовать 3 раза с экспоненциальной задержкой |
| Hiddify install failed | Очистить `/opt/hiddify`, переустановить с флагом `--force` |
| Telegram bot not responding | Пересоздать бота, проверить токен, добавить `/setprivacy` в @BotFather |
| Протокол не подключается из РФ | Переключить `REALITY_DEST` на другой домен из `configs/reality_destinations.json` |

---

## ✅ ЗАВЕРШЕНИЕ

После успешного выполнения:
1. Создать файл `output/DEPLOY_SUCCESS.txt` с:
   - Ссылкой на панель: `https://$PANEL_DOMAIN`
   - Логином/паролем админа
   - Ссылками на подписки для 200 пользователей
2. Отправить уведомление в Telegram админу: "✅ Деплой завершён"
3. Завершить работу агента с кодом 0
